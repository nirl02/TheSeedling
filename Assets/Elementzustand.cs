using Unity.Profiling.Editor;
using UnityEngine;

/*public class Elementzustand : MonoBehaviour
{
    public enum ElementType
    {
        None,
        Plasma,
        LiquidLight,
        CristalisedRocks
    }

    public class Type
    {
        float[][] chart =
        {
           //                    PLA LIQ CRI
           /*PLA*/ /*new float[] { 1f, 2f, 0.5f,},
           /*LIQ*/ /*new float[] { 0.5f, 1f, 2f },
           /*CRI*/ /*new float[] { 2f, 0.5f, 1f }
        };

    }
    
    public static float GetEffectivness(ElementType attackType, ElementType defType)
    {
        if (attackType == ElementType.None || defType == ElementType.None)
        {
            return 1; 
        }

        int row = (int)attackType - 1;
        int col = (int)defType - 1;

        return chart[row][col]; 
    }
}*/
