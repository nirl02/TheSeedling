using UnityEngine;

public class EnergySystem : MonoBehaviour
{
    ThirdPersonDash moveScript;

    void Start()
    {
        moveScript = GetComponent<ThirdPersonDash>();
    }


    // Update is called once per frame
    void Update()
    {
        
    }
}
